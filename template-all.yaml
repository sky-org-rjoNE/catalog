apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: template
  description: A collection of all Backstage templates
spec:
  targets:
    # - ./templates/nodejs/template.yaml
    - ./templates/flask/template.yaml
